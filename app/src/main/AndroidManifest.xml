<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.kelmer.correostracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:name=".CorreosApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CorreosTracker"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.list.ParcelListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.create.CreateActivity"
            android:parentActivityName=".ui.list.ParcelListActivity" />

        <activity
            android:name=".ui.detail.DetailActivity"
            android:parentActivityName=".ui.list.ParcelListActivity" />
        <provider
            tools:replace="android:authorities"
            android:name="androidx.work.impl.WorkManagerInitializer"
            android:authorities="@string/workmanager"
            android:enabled="false" />


    </application>


</manifest>